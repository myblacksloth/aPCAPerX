# ─── Dipendenze del backend ───────────────────────────────────────────────────
# Framework web asincrono per costruire le API REST
fastapi==0.115.12

# Server ASGI ad alte prestazioni che esegue FastAPI
uvicorn[standard]==0.34.2

# Libreria per accettare upload di file in formato multipart/form-data
python-multipart==0.0.20

# Libreria principale per la lettura e l'analisi dei file PCAP/PCAPNG
# Include decoder per tutti i principali protocolli di rete
scapy==2.6.1

# PostgreSQL driver for user/session/passkey persistence
psycopg[binary]==3.2.9

# WebAuthn/FIDO2 helpers for passkey registration and login verification
webauthn==2.5.2
